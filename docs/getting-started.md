# Getting Started

> This is a staging environment. No live systems are connected.

## Prerequisites

- Python 3.11+ (or Node.js 20+ — TBD based on stack choice)
- Git

## Setup

1. Clone the repository
2. Copy the config template:
   ```
   cp configs/example.env configs/.env
   ```
3. Fill in your credentials in `configs/.env` (see [docs/integrations.md](integrations.md))
4. Install dependencies (TBD)
5. Run tests:
   ```
   # TBD
   ```

## Next Steps

- [ ] Choose primary language / runtime
- [ ] Define dependency management (pip / npm / etc.)
- [ ] Implement first integration connector (HubSpot recommended as starting point)
- [ ] Set up CI pipeline
